import { Object3D, Scene, DirectionalLight, PointLight, SpotLight, Color, AnimationMixer } from 'three';
import { PlayerIdentity, PackModeDetails, EventModeDetails, GiftModeDetails } from './PlayerIdentity';

export class PackModeSystem {
  // ... existing code
}

export class EventModeSystem {
  // ... existing code
}

export class GiftModeSystem {
  // ... existing code
}
